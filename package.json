{
  "name": "worknest",
  "version": "0.0.0",
  "private": true,
  "description": "Multi-tenant SaaS project management tool",
  "workspaces": [
    "shared",
    "server",
    "client"
  ],
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "capture:landing": "tsx scripts/capture-landing.ts",
    "capture:ref": "tsx scripts/capture-ref.ts"
  },
  "devDependencies": {
    "playwright": "^1.59.1"
  }
}
